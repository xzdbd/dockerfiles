#!/bin/bash

set -e

psql -f /postgres-dump "$POSTGRES_USER" -U "$POSTGRES_USER" -W "$POSTGRES_PASSWORD"

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" <<-EOSQL
    ALTER ROLE sde WITH PASSWORD '$POSTGRES_PASSWORD';
    ALTER ROLE $POSTGRES_USER WITH PASSWORD '$POSTGRES_PASSWORD';
EOSQL